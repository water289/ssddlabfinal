apiVersion: v2
name: voting-system
version: 0.1.0
appVersion: "0.1.0"
description: Helm chart for Secure Voting System (frontend, backend, database)

dependencies:
  - name: postgresql
    version: "12.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
